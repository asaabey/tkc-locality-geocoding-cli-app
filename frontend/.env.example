# Backend API URL
VITE_API_BASE_URL=http://localhost:8000

# For production on Netlify, set an environment variable named VITE_API_BASE_URL
# to https://aus-geocoding-api.docpockets.com in the Netlify dashboard, or via netlify.toml.